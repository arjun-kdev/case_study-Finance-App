enum loan_status
{
    PENDING =0,APPROVED,REJECTED,PROCESSED,WITHDRAWAL
};
typedef enum loan_status L_STATUS;

enum DB_file_name
{
    CUSTOMER_DB_PATH =0 ,EMPLOYEE_DB_PATH ,LOAN_DB_PATH,RELASE_LOAN_DB_PATH,SCHEME_DB_PATH
};
typedef enum DB_file_name DB_FilePath;
